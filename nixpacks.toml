[phases.setup]
nixPkgs = [
  "python39",
  "cmake",
  "gcc",
  "pkg-config",
  "mesa",
  "glib"
]

[phases.install]
cmds = [
  "cd PulseQ",
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install numpy cmake",
  "python3 -m pip install dlib==19.22.0",
  "python3 -m pip install -r requirements.txt"
]

[start]
cmd = "cd PulseQ && python server/app.py" 